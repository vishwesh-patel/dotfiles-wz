import { Emitter } from "vscode-languageserver-protocol";
export declare class DebouncedEmitter<T> extends Emitter<T> {
    private readonly _delay;
    private _cancelCount;
    private _eventBuffer;
    constructor(delay?: number);
    private addCancel;
    private tryFlush;
    fire(event: T): void;
}
//# sourceMappingURL=debouncedEmitter.d.ts.map