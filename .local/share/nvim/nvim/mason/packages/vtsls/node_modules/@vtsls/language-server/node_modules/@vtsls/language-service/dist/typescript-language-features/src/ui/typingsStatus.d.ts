import { ITypeScriptServiceClient } from '../typescriptService';
import { Disposable } from '../utils/dispose';
export default class TypingsStatus extends Disposable {
    private readonly _acquiringTypings;
    private readonly _client;
    constructor(client: ITypeScriptServiceClient);
    dispose(): void;
    get isAcquiringTypings(): boolean;
    private onBeginInstallTypings;
    private onEndInstallTypings;
}
export declare class AtaProgressReporter extends Disposable {
    private readonly _promises;
    constructor(client: ITypeScriptServiceClient);
    dispose(): void;
    private _onBegin;
    private _onEndOrTimeout;
    private onTypesInstallerInitializationFailed;
}
//# sourceMappingURL=typingsStatus.d.ts.map