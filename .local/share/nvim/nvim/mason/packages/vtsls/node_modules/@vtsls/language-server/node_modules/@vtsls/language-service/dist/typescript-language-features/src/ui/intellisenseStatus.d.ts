import { CommandManager } from '../commands/commandManager';
import { ITypeScriptServiceClient } from '../typescriptService';
import { Disposable } from '../utils/dispose';
import { ActiveJsTsEditorTracker } from './activeJsTsEditorTracker';
export declare class IntellisenseStatus extends Disposable {
    private readonly _client;
    private readonly _activeTextEditorManager;
    readonly openOpenConfigCommandId = "_typescript.openConfig";
    readonly createOrOpenConfigCommandId = "_typescript.createOrOpenConfig";
    private _statusItem?;
    private _ready;
    private _state;
    constructor(_client: ITypeScriptServiceClient, commandManager: CommandManager, _activeTextEditorManager: ActiveJsTsEditorTracker);
    dispose(): void;
    private updateStatus;
    private updateState;
    private ensureStatusItem;
}
//# sourceMappingURL=intellisenseStatus.d.ts.map